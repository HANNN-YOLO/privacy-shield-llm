So, today I'm going to build API Validation and Error Handling for Fast API. Fast API has documentation, so if I parse JSON incorrectly for a request, Fast API will give me a Specific Error notification, such as Incorrect Data Type, Invalid JSON, and Missing Column. So now I'm just building Error Handling for Null Values, so I just provide the arguments in case of an error, what to display, and if correct, it will return a success response.
