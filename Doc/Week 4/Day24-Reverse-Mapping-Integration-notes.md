Today, I am writing code for reverse mapping using Redis within Docker.

My focus is on the front-end: adding a button to `index.html`, initializing that button in `app.js`, and creating the `restoreText` function in `api.js`. Once the front-end is complete, I will move on to the back-end; my priority is to create a route for `/restore` and call it in `main.py`. I also need to update `mapping_service` to retrieve the key from the Redis server and add conditional logic to handle situations where the key is not found (given that I set a 10-minute TTL for the key), including implementing error handling for such cases. Finally, I am placing the business logic in `restore_service.py`, where the route from `restore.py` will call the `restore_text` function from that service. You can see proof of the implementation in the image named `restore response.png` within the Week 4 images section, before final testing.
