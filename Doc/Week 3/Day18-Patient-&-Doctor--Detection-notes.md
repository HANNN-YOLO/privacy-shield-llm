So, today I will write code to detect "Patient" and "Doctor" entities.

The concept involves running a back-end to test responses from Microsoft Presidio. If Presidio detects a `[PERSON]` entity, my code will examine the surrounding text; for instance, if the word "Patient" or "Doctor" is found, the system will capture the 30 characters preceding that name. This allows the text "Patient Jhon Smith" to be converted to `[PATIENT]` and "Doctor Reyhan Rafaidhil" to `[DOCTOR]`. Although Presidio initially detects only `[PERSON]`, my code will transform that into `[PATIENT]` or `[DOCTOR]` based on the context. It is important to note that names must consist of at least two words; if only "Patient HAN" is used, the model might not be able to determine whether "HAN" is a `[PERSON]`, `[ORG]`, or another category. Therefore, a person's name needs to be at least two words long so the model can detect it as `[PERSON]` more accurately. If need my proof so can acces in images section, in week 3 section for name 'Patient & Doctor Detected.png'
