in this day i will testing my detection using pattern regex and documentation in week 2

First Day, Im doned for learned about Regex, pattern Regex, why using regex and how to implemented regex in python.
Second Day, Im build code for Email Detection using regex, so if want my proof so can get access from images section, in week 2 section
Third Day, Im build code for Phone Detection using regex, so if want my proof so can get access from images section, in week 2 section
Four Day, Im build code for Date Detection using regex, so if want my proof so can get access from images section, in week 2 section
Five Day, Im build code for Identity Detection using regex, so in Identity just have for Patient ID, Medical Record Number, Medical ID, Insurance ID, Generic ID, Numeric Identifier
Six Day, Im upgrade my code for redact_service.py just can get full text and can combination any detection just using only regex for example if using regex so just can detect for Email, Phone, Date and Identity, and my output just combination from redact text and detection regex. so if want my proof so can get acces from images section, in week 2 section for name file just 'All Combination Detection Regex.png'
Seven Day, Im just test for any detection using regex and just documented every day and for week
