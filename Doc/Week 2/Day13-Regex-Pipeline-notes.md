Today, I will combine all the detections into a single text.

To do this, I am using a loop to retrieve each detection from the list I initialized; this way, when I input the text, the redaction process can detect all relevant elements using regex and redact the text comprehensively.
