in this day i will code for regex to detect Email

so i just using regex patter for Email just type '\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}\b' because from Email know pattern just username@domain.extension, so if for explanation for \b for know boundary for not half text, next im using [A-Za-z0-9._%+-] for can combination for A-Z, a-z, 0-9, and have . for any character can use for \_ or % or + or - so can accept for username, next have type '+@', if + can just add 1 character so can mix for +@ because any email have 1 for @, next have type '[A-Za-z0-9.-]' for can combination for A-Z, a-z, 0-9 so can accept for domain, next have '+\.', if + can just add 1 charakter for anything and just combination for '+\.' so just only 1 . for any email pattern. so why using \ because . just have any character so just \. for search (.), next [A-Za-z]{2,}, if [A-Za-z] can combination A-Z, a-z and this{2,} for grouping only minimum 2 character
